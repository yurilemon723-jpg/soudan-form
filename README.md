# 無料個別相談会 申込フォーム

タップだけで申し込める1ファイルのフォームです。GitHub Pages で公開しています。

- 公開URL: https://yurilemon723-jpg.github.io/soudan-form/
- 中身は `index.html` の1枚だけ（画像・フォント設定も埋め込み済み）
- 送信先やLINEの設定は `index.html` 先頭の `CONFIG` で指定します（トークン等の秘密情報は含みません）

更新するときは `index.html` を差し替えて push すると、1分ほどで公開URLに反映されます。
